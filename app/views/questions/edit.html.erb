<section class='col-8'>
  <% provide :page_title, t('.title') %>
  <% currently_at %>
  
  <%= turbo_frame_tag @question do %>
    <h1 class='md-4'><%= t('.title') %></h1>
    <%= render partial: 'form', locals: { model: @question, method: :put, tags: @tags } %>
  <% end %>
</section>