<section>
  <% provide :page_title, t('page.home') %>
  <% currently_at t('page.home') %>
  <h1 class='md-2'><%= t('page.greeting') %></h1>

    &copy: @elenasu <%= Time.now.year %>

    <p><%= link_to 'Admin page', admin_users_path %></p>
</section>